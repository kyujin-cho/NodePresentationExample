extends layout
block content
    ul.messages
        for message in messages
            li.message!= message.message
    form#message-form(action="/messages", method="post")
        textarea(placeholder="Insert a message", name="message", onkeydown="if (event.keyCode == 13) document.getElementById('message-form').submit();")